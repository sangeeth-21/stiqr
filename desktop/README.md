# Desktop Application - Flutter Desktop (Windows/macOS/Linux)

Cross-platform desktop application with one shared codebase, excellent performance, hardware integration, printing, POS peripherals, and lower resource usage than Electron.

## Run Application
```bash
flutter run -d macos # or windows / linux
```
