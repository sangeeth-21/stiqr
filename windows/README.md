# Windows Application - Flutter Windows

Native Windows desktop application supporting printers, barcode scanners, cash drawers, serial devices, USB peripherals, and offline-first capabilities.

## Run Application
```bash
flutter run -d windows
```
