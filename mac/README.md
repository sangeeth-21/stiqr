# macOS Application - Flutter macOS or SwiftUI

Flutter macOS provides a shared codebase with other platforms. SwiftUI is recommended only when deep macOS integration or Apple-native functionality is required.

## Run Application
```bash
flutter run -d macos
```
